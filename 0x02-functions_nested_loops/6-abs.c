#include "main.h"

/**
 *  _abs - checks if a number is absolute if not makes it so
 *  @n: number to check
 *  Return: absolute n
 */

int _abs(int n)
{
	if (n >= 0)
	{
	return (n);
	}
	return (-n);
}
