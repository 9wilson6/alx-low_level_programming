#include "main.h"

/**
 * main - test if a function that checks if number is
 * positive or negative works when 0 is passed as the number.
 * Return: 0 (success)
 */

int main(void)
{
	positive_or_negative(0);

	return (0);
}
