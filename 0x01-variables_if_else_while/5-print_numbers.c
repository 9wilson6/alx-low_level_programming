#include <stdio.h>

/**
 * main - print allsingle digit integers
 * Return: 0 for successful execution
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		printf("%d", num);
		printf("\n");
	}
	return (0);
}
