#include <stdio.h>


/**
 *  main - print numbers of base 16
 *  Return: 0 for successful run
 */
int main(void)
{
	int num;
	char ch;

	for (num = 48; num < 58; num++)
		putchar(num);
	for (ch = 'a'; ch < ch; ch++)
		putchar(ch);
	putchar('\n');
	return (0);
}
